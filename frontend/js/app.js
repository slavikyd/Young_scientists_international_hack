document.addEventListener('DOMContentLoaded', () => {
    console.log('🚀 Certificate Generator GlobalCertHub loaded');
    
    // All managers are initialized via their individual DOMContentLoaded listeners
    // UI, Upload, Templates, and Generate managers will auto-initialize
});